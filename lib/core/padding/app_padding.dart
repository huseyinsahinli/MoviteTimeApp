import 'package:flutter/material.dart';

class AppPadding {
  static const pageTitlePadding =
      EdgeInsets.symmetric(horizontal: 16, vertical: 8);

  static const movieTitlePadding = EdgeInsets.only(
    bottom: 15,
    left: 15,
  );
}
